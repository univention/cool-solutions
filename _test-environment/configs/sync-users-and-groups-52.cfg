[Global]
# These settings can be overwritten by the instance
logfile: autotest.log

proxmox_node: uni-pve-03
proxmox_template_pool: Images
proxmox_pool: PS
proxmox_bridge: PS
proxmox_extra_label: [ENV:JOB_BASE_NAME]
proxmox_vcpus: 2
proxmox_memory: 4096

environment:
 UCS_VERSION=[ENV:UCS_VERSION]

# pdn-dest and pdn-source are two INDEPENDENT domains (each its own
# domaincontroller_master), not a primary/replica pair -- the sync package
# pushes user/group objects from one standalone domain to another over
# SSH/rsync, so both sections get their own standalone pre-joined primary.

[pdn-dest]
proxmox_template: template-ucs-joined-5.2-6-primary
command1:
 LOCAL echo "[pdn-dest]" >> "inventory.ini"
 LOCAL echo "pdn-dest ansible_host=[pdn-dest_IP]" >> "inventory.ini"
 PUT /setup/ssh_ansible_key.pub /root/.ssh/authorized_keys
 chmod 600 /root/.ssh/authorized_keys
 . utils.sh; basic_setup
 ucr set nameserver1=10.208.1.15 nameserver2=10.208.1.16 dns/forwarder1=10.208.1.15 dns/forwarder2=10.208.1.16
 systemctl restart named || true
 # template is pre-joined -- no-op on the primary, kept for uniformity
 . utils.sh; basic_setup_ucs_joined "[pdn-dest_IP]"
 . utils.sh; rotate_logfiles
 . utils.sh; add_tech_key_authorized_keys
 . utils.sh; upgrade_to_latest_errata
command2:
 # no join step: template-ucs-joined-5.2-6-primary is already a fully provisioned domain
files:
 _test-environment/utils/utils.sh /root/

[pdn-source]
proxmox_template: template-ucs-joined-5.2-6-primary
command1:
 LOCAL echo "[pdn-source]" >> "inventory.ini"
 LOCAL echo "pdn-source ansible_host=[pdn-source_IP]" >> "inventory.ini"
 PUT /setup/ssh_ansible_key.pub /root/.ssh/authorized_keys
 chmod 600 /root/.ssh/authorized_keys
 . utils.sh; basic_setup
 ucr set nameserver1=10.208.1.15 nameserver2=10.208.1.16 dns/forwarder1=10.208.1.15 dns/forwarder2=10.208.1.16
 systemctl restart named || true
 # template is pre-joined -- no-op on the primary, kept for uniformity
 . utils.sh; basic_setup_ucs_joined "[pdn-source_IP]"
 . utils.sh; rotate_logfiles
 . utils.sh; add_tech_key_authorized_keys
 . utils.sh; upgrade_to_latest_errata
command2:
 # no join step: template-ucs-joined-5.2-6-primary is already a fully provisioned domain
files:
 _test-environment/utils/utils.sh /root/

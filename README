Leider kann der Default-Branch in Plan.io nicht festgelegt werden, deshalb wird
immer "master" ausgecheckt.

Um zu einem richtigen Branch mit Inhalt zu kommen, führe folgende Befehle aus:

Alle verfügbaren Branches anzeigen:
git branch -a

Gewünschten Branch auschecken, dabei muss "origin/" vor dem neuen Branchnamen
entfernt werden:
git checkout -b BRANCH origin/BRANCH

Alternativ kann das Script ./switch-to-latest-branch ausgeführt werden.
